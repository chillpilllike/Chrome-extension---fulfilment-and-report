# Nutricity Amazon Invoice Uploader

Load this folder as an unpacked Chrome extension. It opens Amazon order detail pages, clicks the invoice menu, follows **Printable Order Summary**, downloads the direct `order-document.pdf`, renames it by Odoo order, and uploads it to the app.

The app must be running and reachable at the API base configured in the popup.
