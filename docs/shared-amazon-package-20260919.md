# Shared Amazon parcel capture

Amazon order 113-5491407-3133014 (NC26931) was repeatedly captured without a visible pickup tracking ID. Live inspection in Amit's Chrome profile confirmed Amazon's package page explicitly lists two purchases, this order and 113-0764624-1889004, in TBA334522100431. Each purchase has a different Amazon shipment ID (Nfr8Nl1Yc / NWrDNH1Yc). The second capture now contains the physical barcode; the purchase ownership guard classified the second association as conflicting.

Keep both purchase records and their own URLs. Link their canonical physical barcode only when separate persisted captures, scoped to each purchase, contain the same physical tracking ID and match each purchase's shipment ASIN, within the same store/Odoo order. A related-order link, bare barcode collision, mismatched ASIN, or foreign order URL cannot establish this. Existing scan ownership is unchanged. Counting and display merge proven same-parcel associations and retain both line IDs and Amazon references. Future barcode scans already update canonical parcel associations together.

Validation: exact NC26931 PostgreSQL replay with three alternating individual and bulk refreshes; pickup response has one received Amazon parcel and two pending third-party parcels. Original September 15 receipt remains unchanged. New PostgreSQL tests cover missing independent evidence, wrong ASIN/order, repeated refresh, and merged display metadata. Frontend production build passed. Pickup/dispatch suite: 99 passed, two optional PostgreSQL tests skipped, one existing internal-label test failure reproduced on unchanged baseline; new PostgreSQL suite passed separately.
